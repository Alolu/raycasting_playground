import { Scene, SceneRenderingContext } from "../engine/scene";

export default class Test implements Scene {
  backgroundColor = "red"

  draw(sceneContext: SceneRenderingContext): void {
    
  }
  
}